Place Piper voice models under this folder.

Example:
  voices/es_ES/model.onnx
  voices/es_ES/model.onnx.json
  voices/en_US/model.onnx
  voices/en_US/model.onnx.json

Every .onnx file must have a matching .onnx.json file.
Subfolders are scanned recursively when the application starts.
